{% extends 'layout.html.twig' %}

{% block body %}
    {% if subject is not null %}
        {{ 'Greetings' | trans }} {{ subject }}!
    {% else %}
        {{ 'about.description' | trans }}
    {% endif %}
{% endblock %}